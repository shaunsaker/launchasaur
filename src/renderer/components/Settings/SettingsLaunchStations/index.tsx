import React, { ReactElement } from "react";
import { SettingsBase } from "../SettingsBase";

export const SettingsLaunchStations = (): ReactElement => {
  return <SettingsBase>LAUNCH STATIONS</SettingsBase>;
};
